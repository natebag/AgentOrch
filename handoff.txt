AgentOrch CLI expansion status as of 2026-03-30: added spawn/UI support for Gemini CLI, GitHub Copilot CLI, and Grok CLI (marked experimental). AgentConfig now has optional `experimental` flag, and presets preserve `model` plus `experimental`. Backend launch wiring currently supports: Gemini via `gemini mcp remove/add ...` then `gemini [--model] [--yolo]`; Copilot via `copilot --additional-mcp-config @<path>` plus optional `--model=` and `--allow-all`; Grok is launch-only experimental support with model passthrough, but MCP auto-registration is not solved yet. Build passed and targeted unit tests passed. Reviewer approved with minor notes: verify exact Gemini/Copilot flag syntax in live testing, and confirm Grok remains clearly experimental/partial. Next recommended step is live validation by spawning Gemini, Copilot, and Grok agents, checking MCP tool availability, model flag behavior, auto-approve behavior, and preset save/load persistence.